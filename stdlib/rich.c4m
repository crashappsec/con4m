extern callflattable(a: ptr) -> ptr {
  local: table(list[rich]) -> rich
  pure: true
}

extern calltable(a: ptr) -> ptr {
  local: table(list[list[rich]]) -> rich
  pure: true
}

extern ropeplus(a: ptr, b: ptr) -> ptr {
  local: __plus__(rich, rich) -> rich
  pure: true
}
